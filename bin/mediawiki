#!/usr/bin/env node
var server = require('../');

var argv = require('yargs')
    .usage('Usage: $0 -p [port]')
    .demand('p')
    .alias('p', 'port')
    .nargs('p', 1)
    .describe('p', 'Server port')
    .help('h')
    .alias('h', 'help')
    .argv;

server.makeServer(+argv.port);
